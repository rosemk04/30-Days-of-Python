'''AUTHOR=Rose Mary
DESCRIPTION=Calculate the Area of a Rectangle Objective: 
Write a Python program that takes the length and width of a rectangle as inputs and calculates its area.
'''
length=float(input("Enter the length:"))
width=float(input("Enter the breadth:"))
print("The area of a rectangle is:",length*width)
